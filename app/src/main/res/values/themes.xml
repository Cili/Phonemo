<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Phonemo" parent="Theme.AppCompat.DayNight.DarkActionBar"> <!-- Changed from Material - is now AppCompat-->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/phonemared</item>
        <!-- Secondary brand color. -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textViewStyle" tools:targetApi="l">@font/capriola</item>
    </style>
</resources>